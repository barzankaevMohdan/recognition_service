# Recognition Service

Python сервис для распознавания лиц в реальном времени с использованием InsightFace.

## 🎯 Назначение

Recognition Service отвечает за:
- Распознавание лиц на видеопотоках с камер
- Сопоставление лиц с базой сотрудников
- Отправку событий распознавания в Backend
- Отслеживание присутствия сотрудников
- Предоставление отладочного видео

## 🛠️ Технологический стек

- **Python 3.10/3.11** - Runtime
- **InsightFace** - Распознавание лиц
- **ONNX Runtime** - Выполнение моделей
- **OpenCV** - Обработка видео
- **NumPy** - Математические операции
- **Flask** - Отладочное API
- **Requests** - HTTP клиент

## 📁 Структура

```
recognition_service/
├── recognition/             # Модули распознавания
│   ├── matching.py         # Сопоставление лиц
│   ├── preprocessing.py    # Предобработка
│   ├── presence.py         # Логика присутствия
│   ├── quality.py          # Оценка качества
│   └── tracker.py          # Отслеживание
├── utils/
│   ├── cache.py           # Кэширование
│   └── timing.py          # Замеры времени
├── main.py                # Multi-camera mode
├── face_app.py            # InsightFace модель
├── camera.py              # Работа с камерой
├── employees.py           # Работа с сотрудниками
├── events.py              # Отправка событий
└── requirements.txt
```

## 🚀 Быстрый старт

Смотрите инструкции по установке:
- [macOS](./SETUP_MACOS.md)
- [Windows](./SETUP_WINDOWS.md)

## 🧠 Алгоритм распознавания

### 1. Инициализация
```python
# Загрузка модели InsightFace (buffalo_l)
app = insightface.app.FaceAnalysis()
app.prepare(ctx_id=0)

# Загрузка эмбеддингов сотрудников из Backend
employees = fetch_employees()
```

### 2. Обработка кадра
```python
while True:
    # 1. Получить кадр с камеры
    frame = cap.read()
    
    # 2. Детекция лиц
    faces = app.get(frame)
    
    # 3. Для каждого лица:
    for face in faces:
        # 3.1 Извлечь эмбеддинг (512D вектор)
        embedding = face.embedding
        
        # 3.2 Сравнить с базой (cosine similarity)
        employee = match_employee(embedding, employees)
        
        # 3.3 Обновить трекер
        tracker.update(face, employee)
        
        # 3.4 Проверить cooldown
        if should_send_event(employee):
            # 3.5 Отправить событие в Backend
            send_event(employee, camera, timestamp)
```

### 3. Сопоставление
```python
def match_employee(embedding, employees):
    similarities = []
    for emp in employees:
        # Косинусное сходство
        sim = cosine_similarity(embedding, emp.embedding)
        similarities.append((emp, sim))
    
    # Порог 0.5 (настраивается)
    best_match = max(similarities, key=lambda x: x[1])
    if best_match[1] > 0.5:
        return best_match[0]
    return None
```

## 📊 Производительность

### Характеристики
- **FPS:** 5-10 кадров/сек на камеру
- **Latency:** ~100-200ms на кадр
- **Точность:** >95% при хорошем освещении
- **Эмбеддинг:** 512 размерность

### Требования к железу
**Минимум:**
- CPU: 4 ядра
- RAM: 4GB
- Камеры: до 2-3

**Рекомендуется:**
- CPU: 8+ ядер
- RAM: 8GB+
- GPU: NVIDIA (опционально)
- Камеры: до 10+

## 🎥 Работа с камерами

### Multi-Camera режим
```bash
python -m recognition_service.main
```

**Что происходит:**
1. Сервис опрашивает Backend каждые 60 сек
2. Получает список активных камер компании
3. Запускает отдельный поток для каждой камеры
4. Автоматически добавляет новые камеры
5. Останавливает неактивные

### Отладочное видео
Каждая камера предоставляет Flask endpoint:
```
http://localhost:500{camera_id}/video_feed
```

Показывает:
- Bounding boxes вокруг лиц
- Имена распознанных сотрудников
- Confidence score
- FPS

## 🔧 Конфигурация

### Переменные окружения (.env)
```env
COMPANY_SLUG=demo-company       # Компания для обработки
BACKEND_URL=http://localhost:3000
REFRESH_INTERVAL=60             # Обновление списка камер
LOG_LEVEL=INFO                  # DEBUG, INFO, WARNING, ERROR
```

### Параметры распознавания
```python
# config.py
SIMILARITY_THRESHOLD = 0.5      # Порог сопоставления (0-1)
COOLDOWN_SECONDS = 300          # Cooldown между событиями (5 мин)
MAX_FACE_AGE = 30               # Удаление старых треков (сек)
QUALITY_THRESHOLD = 0.5         # Минимальное качество лица
```

## 🔄 Интеграция

### С Backend
```python
# Получение сотрудников
GET /employees?companySlug={slug}

# Отправка события
POST /events
{
  "employeeId": 1,
  "cameraId": 1,
  "confidence": 0.95,
  "timestamp": "2024-01-01T10:00:00Z"
}
```

### С Camera Gateway
```python
# Подключение к MJPEG потоку
stream_url = f"http://localhost:4000/streams/{camera_id}.mjpg"
cap = cv2.VideoCapture(stream_url)
```

## 🐛 Troubleshooting

### Низкая точность
- ✅ Проверьте качество видео с камеры
- ✅ Улучшите освещение
- ✅ Загрузите больше фото сотрудников
- ✅ Уменьшите `SIMILARITY_THRESHOLD`

### Низкая производительность
- ✅ Уменьшите разрешение видео в Camera Gateway
- ✅ Увеличьте интервал обработки кадров
- ✅ Используйте GPU (onnxruntime-gpu)

### Много ложных срабатываний
- ✅ Увеличьте `SIMILARITY_THRESHOLD`
- ✅ Увеличьте `QUALITY_THRESHOLD`
- ✅ Проверьте качество фото в базе

## 📚 Модели

**InsightFace buffalo_l:**
- Размер: ~200MB
- Точность: Very High
- Скорость: Medium
- Автоматически загружается при первом запуске

---

Для детальной настройки смотрите [SETUP_MACOS.md](./SETUP_MACOS.md) или [SETUP_WINDOWS.md](./SETUP_WINDOWS.md)
# infra
# recognition_service
